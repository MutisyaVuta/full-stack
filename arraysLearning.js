//display how effective arrays are in keeping data organized
const winningNumbers = [2, 4, 6, 8, 10];
function logWinningNumbers(numbers) {
    console.log("Winning numbers are:", numbers);
}
logWinningNumbers(winningNumbers);